package com.Morph.logisticspipes.routing;

/** Ported directly from LP 1.12.2 — no changes needed. */
public enum PipeRoutingConnectionType {
    canRouteTo,
    canRequestFrom,
    canPowerFrom,
    canPowerSubSystemFrom;
}
