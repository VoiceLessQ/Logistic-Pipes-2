# Done to increase the memory available to Gradle.
org.gradle.jvmargs=-Xmx2G -Dloom.nojavadoc=true
org.gradle.parallel=true

# This project targets Java 21 — override the per-user default in
# ~/.gradle/gradle.properties (which pins to JDK 25 for other projects).
# Adjust this path if your JDK 21 lives elsewhere.
org.gradle.java.home=F:\\Minecraft modding\\Tools\\jdk-21\\jdk-21.0.9+10

# Mod properties
mod_version = 1.0.0
maven_group = com.Morph
archives_name = lp2
author = NoZeroG
enabled_platforms = fabric,neoforge

# Minecraft properties
minecraft_version = 1.21.1

# Dependencies
fabric_loader_version = 0.16.9
fabric_api_version = 0.116.0+1.21.1
neoforge_version = 21.1.172

# Team Reborn Energy — Fabric RF/FE interop (Power Junction energy import).
# API package: team.reborn.energy.api.*
tr_energy_version = 4.0.0
